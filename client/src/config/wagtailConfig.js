export const ADMIN_API = window.wagtailConfig.ADMIN_API;
export const STRINGS = window.wagtailConfig.STRINGS;
export const ADMIN_URLS = window.wagtailConfig.ADMIN_URLS;

// Maximum number of pages to load inside the explorer menu.
export const MAX_EXPLORER_PAGES = 200;
